# Installation

```bash
git clone https://github.com/mendableai/firecrawl.git
cp -r ./nuq-postgres ./firecrawl/nuq-postgres
cd firecrawl
docker compose build nuq-postgres
docker compose up -d
```

# Usage
